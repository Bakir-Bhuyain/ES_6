//filter hocche ami ekta sorto dibo and filter shei shorter basis e hocche amk jinish gula return korbe ;
//cheke cheke dibe ;

const numbers = [4, 5, 6, 10, 12, 13, 14,  8, 2, 3, 18, 19];

const greaterThen = numbers.filter(x => x > 10);
console.log(greaterThen);

const names = ['bakir', 'rakib', 'abir', 'tusher', 'fahim'];
const length = names.filter(lenPositon => lenPositon.length <= 4 );
console.log(length);